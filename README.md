the project is an interpreter for the Airbnb clone herein referenced as hbnb

## how to strt the interpreter

the interpreter is sarted with calling the roootfile `python console.py`

basic commands are;

```
git ststus
git add
git commit
```
